CREATE TABLE InstitutionEntity (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    logoUrl TEXT
);

selectAll:
SELECT * FROM InstitutionEntity;

insert:
INSERT OR REPLACE INTO InstitutionEntity(id, name, logoUrl)
VALUES (?, ?, ?);
