name: Main

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - '**.html'
      - 'media/**'
      - 'docs/**'

concurrency:
  group: main-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    uses: ./.github/workflows/build.yml
